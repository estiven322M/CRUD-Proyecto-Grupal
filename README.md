# Proyecto Grupal CRUD

Este proyecto desarrollado en **Java** utiliza el **patrón de arquitectura MVVM (Model-View-ViewModel)**, simulando el funcionamiento del framework **TD-MBUID**.  
El objetivo es gestionar operaciones **CRUD (Crear, Leer, Actualizar y Eliminar)** 
## 🧩 Arquitectura
El modelo **MVVM** implementado en este proyecto se organiza de la siguiente manera:

- **Model**: Contiene la lógica de negocio y las clases que representan las entidades (clase `Reserva`).
- **ViewModel**: Actúa como intermediario entre la vista y el modelo, gestionando la comunicación y el flujo de datos (clase `GestionReservasViewModel`).
- **View**: Implementada con **Java Swing**, representa la interfaz de usuario (clase `FormularioCrudReserva`).

Esta estructura **simula el framework TD-MBUID**


